spring.datasource.url=jdbc:mariadb://${DB_HOST:localhost}:${DB_PORT:3306}/${DB_NAME:roomdb}
spring.datasource.username=${DB_USER:room_user}
spring.datasource.password=${DB_PASSWORD:room_pass}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
server.port=8080
